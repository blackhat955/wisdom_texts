spring:
  datasource:
    url: jdbc:postgresql://localhost:5439/wisdomtexts
    username: developer
    password: mypassword


  jpa:
    hibernate:
      ddl-auto: update  # can be none, validate, update, create, create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect


server:
  port: 8080